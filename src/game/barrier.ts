/**
 * Барьер — защитное умение героя (слот 3 хотбара, клавиша «3»).
 *
 * Не снаряд и не прицел: мгновенно вешает на себя щит, который на несколько секунд
 * режет входящий урон. Сам расчёт урона — в combat.ts (playerDamageTaken), поэтому
 * здесь только числа и НИ ОДНОГО импорта Phaser: так константы видны и тестам,
 * которые гоняются в node без движка.
 */

/** Сколько держится щит, мс. */
export const BARRIER_DURATION = 5000;
/** Насколько режет входящий урон: 0.8 — на 80%, до героя доходит пятая часть. */
export const BARRIER_REDUCTION = 0.8;
/** Стоимость маны. */
export const BARRIER_MP_COST = 15;
/** Перезарядка, мс. Заметно длиннее щита — защита не должна висеть всегда. */
export const BARRIER_COOLDOWN = 14000;
